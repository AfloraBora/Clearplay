# Local fixes — browser verification notes

Brownout 1.2.1: drawer Open source links reveal the requested post and its quote in the destination tab. Other posts remain filtered. A URL fragment carries the explicit reveal and is not transmitted in the HTTP request. Added invalidated-context guard. 22 automated tests passed, including destination reveal scope. User reported “Uncaught (in promise) Error: Extension context invalidated”; this indicates an old page script after an extension update/reload. Refresh existing X tabs.

Clearplay 1.2.0 candidate: the original 720p failure was reproduced in Chrome on a source offering 1080p/1440p/2160p. Old direct quality setters were removed. The candidate uses YouTube's native English Quality menu, selects once per video/preference transition, and switches to Auto once when bypassed. It never calls page reload or loadVideo methods. Status checks actual decoded dimensions. 9 revised automated tests passed. These tests are not a substitute for live validation.

Live Chrome testing on 21 September: found and corrected a native-menu text match (YouTube renders Quality720p without a separating space). The older quality extension was still installed; the user removed it. After that removal, Clearplay automatically reached decoded 1080p on both IFSh7H8Zj8c and wUVvQk7XLd4. On the latter, bypass/restore preserved playback progression (16 → 27 → 35 → 72 seconds), with 1080p confirmed after restore. No recurring reset observed. A fullscreen control was exercised, but fullscreen entry itself was not conclusively verified. Brownout lookup connection recovered after opening X's About this account page. Brownout destination reveal remains covered by automated tests; live verification is still in progress.

Files copied into the existing local installation directories. Extension reload and X/YouTube page refresh are required. No store submission or public repository was modified. User has been asked to reload both local extensions and disable the previous YouTube quality extension during testing.
